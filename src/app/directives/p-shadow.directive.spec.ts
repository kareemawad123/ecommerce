import { PShadowDirective } from './p-shadow.directive';

describe('PShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new PShadowDirective();
    expect(directive).toBeTruthy();
  });
});
